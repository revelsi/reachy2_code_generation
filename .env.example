# OpenAI API key (required)
# Replace with your actual OpenAI API key
OPENAI_API_KEY=

# --- Model Selection ---
# Set the currently active models here.
# These values override the defaults defined in config.py.
# Ensure the chosen models are listed in AVAILABLE_MODELS in config.py.
# Examples: gpt-4.1-mini, gpt-4o, o3-mini, o1
MODEL=gpt-4.1-mini
EVALUATOR_MODEL=gpt-4.1-nano

# --- Model Parameters (Optional) ---
# These override the defaults set in config.py for the selected model's family.

# Parameter applicable to GPT models ONLY:
MODEL_TEMPERATURE=0.2

# Parameters specific to GPT family (gpt-*, etc.):
MODEL_MAX_TOKENS=4000
MODEL_TOP_P=0.95
MODEL_FREQUENCY_PENALTY=0
MODEL_PRESENCE_PENALTY=0

# Parameters specific to O-family (o1, o3, etc.):
MODEL_MAX_COMPLETION_TOKENS=4000
# Controls thinking time/resources. Allowed values: "low", "medium", "high"
MODEL_REASONING_EFFORT=medium

# --- Reachy Settings ---
# Use the IP address of your physical robot for physical mode
# For simulation (Docker), use localhost
REACHY_HOST=localhost

# --- WebSocket Server (Optional) ---
# Host and port for the WebSocket server used for UI notifications
WS_HOST=0.0.0.0
WS_PORT=8765

# --- Debug Mode (Optional) ---
# Set to true to enable debug logging
DEBUG=false 